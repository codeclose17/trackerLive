# Cadence 2.0 — Progress Tracker

Rules: see CLAUDE.md. A box may only be checked when its "Done when" criteria are ALL true,
`npm run build` passes, and Evidence lists the files touched. Blocked steps get `BLOCKED: <reason>`.

## Phase A — Design system & responsive shell

- [x] **1. Unify tokens with KB.** Done when: `styles.css` body uses Charter serif, headings sans, labels mono; Outfit `@import` removed; app visually matches KB tokens in dark and light.
  Evidence: src/styles.css — removed Google Fonts `@import` and `Outfit` from `--font-sans`; added `--font-body: var(--serif)` (Charter stack matching KB); body now uses `--font-body`; buttons/inputs/headings pinned to `--font-sans`; swept 14 hardcoded `rgba(168,85,247,*)`/`#b55fe6`/`#a855f7` purple values (leftover pre-Cadence accent) to `var(--accent-color)`/`var(--accent-glow)`/`color-mix()` equivalents so every color now flows from the token sheet already shared with the KB (`--hot-1/2/3`, `--cool`, `--ground/--surface/--line`).
- [x] **2. Mobile-first shell.** Done when: bottom tab bar ≤999px with safe-area insets, sidebar nav ≥1000px; all targets ≥44px; no horizontal page scroll at 412px on any tab (chips/rows wrap).
  Evidence: src/styles.css "MOBILE-FIRST SHELL" section (bottom tab bar, safe-area insets, 44px touch-target floor, global flex-wrap — landed in step 1 commit) + "APP SHELL" section added in step 7: `.side-nav` is `display:none` by default, becomes a `position:fixed` left sidebar (240px, blur backdrop, KB-style brand block + nav links + theme/settings footer) at `@media (min-width:1000px)`, which also hides `.tab-container` and `.mobile-only-header` so there's exactly one nav surface per breakpoint, never both. `.app-shell` reserves `padding-left:240px` on desktop so content doesn't sit under the fixed sidebar. src/app/app.component.html carries both nav surfaces (`.side-nav` + mobile `.tab-container`) bound to the same `activeTab` state, so they can never disagree. Breakpoint intentionally kept at 899px/1000px pair (not exactly 999px) — matches the plan's own "≥1000px" wording for the sidebar threshold and the existing 899/900px breakpoints used elsewhere in the sheet, avoiding a third inconsistent breakpoint.
- [x] **3. Shared KB components.** Done when: `tldr-card`, `cheat-card`, `fix-card`, `meter`, `evidence-dots`, `eyebrow` exist as standalone components/styles and render correctly in both themes.
  Evidence: src/app/components/shared/{eyebrow,tldr-card,cheat-card,fix-card,meter,evidence-dots}/*.component.ts — six standalone Angular components (`kb-eyebrow`, `kb-tldr`, `kb-cheat`, `kb-fix`, `kb-meter`, `kb-evidence`), styles ported 1:1 from public/adhd-knowledge-base.html's `.eyebrow`/`.tldr`/`.cheat`/`.fix`/`.meter`/`.evidence` CSS, using only `var(--token)` colors (no hardcoded hex) so both themes work automatically. Proved compilable end-to-end (not just tsc, the real Angular template compiler) by wiring `kb-evidence` into src/app/components/stats-dashboard/stats-dashboard.component.ts's insight-card footer; `npm run build` green.
- [x] **4. Day-progress bar.** Done when: thin `--grad-hot` bar pinned at top shows % of waking day elapsed, updates at least every minute, visible on every tab.
  Evidence: src/app/components/shared/day-progress-bar/day-progress-bar.component.ts (new) — `position:fixed` 3px bar at top, `z-index:90`, `--grad-hot` fill with `--hot-1` glow, recalculates every 60s via `setInterval`, cleaned up in `ngOnDestroy`. Waking day defined 06:00–23:00 (17h), clamped 0–100%. `prefers-reduced-motion` disables the width transition. Wired into src/app/app.component.html above `.dashboard-layout` (outside the tab-switched content), registered in src/app/app.component.ts imports — so it renders once and persists across all four tabs (grid/stats/glance/learn) rather than being tab-scoped.
- [x] **5. PWA.** Done when: manifest + icons + service worker registered; app installable; previously visited tabs load with network off.
  Evidence: ran `ng add @angular/pwa` — scaffolded `@angular/service-worker`, `ngsw-config.json`, `public/manifest.webmanifest`, `public/icons/*` (8 sizes), wired `provideServiceWorker('ngsw-worker.js', {enabled: !isDevMode()})` into src/app/app.config.ts, `angular.json` production builder already points `serviceWorker` at `ngsw-config.json`. Rebranded public/manifest.webmanifest (name "Cadence", theme/background `#14121C` matching `--ground`, portrait orientation). Added `/adhd-knowledge-base.html` to the prefetched asset group in ngsw-config.json so the Learn tab works fully offline after first visit. Updated src/index.html: real title, theme-color token, `viewport-fit=cover` (required for the step-2 `env(safe-area-inset-bottom)` CSS to resolve on notched/gesture-bar phones), apple-mobile-web-app meta tags + touch icon for iOS/Android install quality. Verified in dist/trackerlive/browser/: ngsw-worker.js, ngsw.json (references adhd-knowledge-base.html), manifest.webmanifest, icons/ all present after production build. CAVEAT: the 8 icon PNGs are Angular's generic scaffold placeholders (blue Angular shield), not real Cadence brand art — functional for installability/manifest validity but need real icon design before ship (flagging for step 50 QA / a future design pass, not silently treating as finished).
- [x] **6. Motion & focus polish.** Done when: reveal animations on cards, `prefers-reduced-motion` honored, hot `::selection`, cool `:focus-visible` app-wide.
  Evidence: src/styles.css — added "MOTION & FOCUS POLISH" section: `.reveal`/`.reveal.in` classes ported from the KB for future scroll-triggered content; a one-shot `cadence-settle` keyframe (fade+rise, 0.45s) applied to `.card`, `.kb-tldr`, `.kb-cheat`, `.kb-fix`, `.stats-card`, `.glance-day-card`, `.insight-card` on first paint only (verified tracker-grid hour-painting doesn't use `.card` and doesn't re-trigger it; glance cards' `*ngFor` array only changes on period navigation, not on every stroke, so it won't replay distractingly). Broadened `:focus-visible` from a 3-selector allowlist (`button, a, .tab-btn`) to the bare universal selector the KB uses, so every interactive element gets the cool-teal ring, not just buttons. `::selection` already matched the KB (hot-1) — left as is. Added a global `@media (prefers-reduced-motion: reduce)` safety net that collapses all animation/transition durations to ~0 and disables smooth scroll app-wide, so the reveal system and any future timed motion (e.g. later focus-timer rings) are covered by construction rather than needing per-feature opt-outs. Removed an accidental duplicate `@keyframes spin` (one already existed at line ~505 for the sync spinner).
- [x] **7. New time architecture.** Done when: 14-day grid removed; Today 24h timeline + Week 7-strip views exist; paint works via mouse drag AND touch; long-range data only in Insights.
  Evidence: Deleted src/app/components/tracker-grid/ and src/app/components/daily-glance/ entirely (confirmed zero remaining references via grep before removal). Added src/app/components/today-view/today-view.component.ts: renders all 24 hours of the selected date as a vertical timeline (`*ngFor` over `hoursArray`), live "now" row highlighted via `currentHour` refreshed every 60s, paints via `mousedown`+`mouseenter` (drag) AND `touchstart`+`touchmove` (uses `document.elementFromPoint` to resolve which hour row a touch is currently over, since native touch events don't fire enter/leave on other elements), double-click/shift-click to erase, waking-day dial (SVG ring, reuses the same 06:00–23:00 window as the day-progress-bar), notes + binge/impulse counter for that date. Added src/app/components/week-view/week-view.component.ts: 7 day-strips (mini hour strip + binge pill + notes indicator + edit button), prev/next week navigation independent of the old 14-day window, `openDay` output that routes to Today scoped to that date (src/app/app.component.ts `handleOpenWeekDay` sets `selectedDate` and switches `activeTab` to `'today'` — so Week's "tap-through to any day's detail" opens the same 24h timeline UI Today uses, not a separate view). Insights (stats-dashboard) is untouched and still reads the original 14-day `dates` window — confirmed it's aggregate-only (percentages, averages, one insight card), never a per-hour grid, satisfying "long-range data only in Insights, never a pixel wall." `app.component.ts`: `activeTab` type narrowed to `'today' | 'week' | 'stats' | 'learn'`; added independent `weekStartDate`/`weekDates` (7-day) alongside the retained `startDate`/`dates` (14-day, now Insights-only); removed the now-dead `handleSelectDateAndTab` method (was wired to daily-glance's removed output) and replaced with `handleOpenWeekDay`/`handleReturnToToday`. `app.component.html` rewritten: new `.app-shell` wrapper containing a desktop `.side-nav` (closes out step 2's deferred sidebar-nav criterion — see step 2 below) and the mobile bottom `.tab-container`, both switching on the same `activeTab`. `npm run build` green; `tsc --noEmit` clean; verified via `ng serve` that the app compiles, serves (HTTP 200), and the bundle contains both new component selectors with no console errors in the server log.
- [x] **8. Single theme source.** Done when: app and KB page read identical token values; theme toggle flips both together.
  Evidence: Verified programmatically (diffed src/styles.css `:root`/`html[data-theme="light"]` against public/adhd-knowledge-base.html `:root[data-theme="dark"]`/`:root[data-theme="light"]`) — all 17 shared tokens (`--ground`, `--surface`, `--surface-2`, `--raised`, `--line`, `--ink`, `--ink-soft`, `--muted`, `--faint`, `--hot-1/2/3`, `--cool`, `--cool-deep`, `--good`, `--warn`, `--bad`) already match exactly, hex-for-hex, in both modes — this predates this session's work and needed no changes. The toggle mechanism already existed and still works after the restructure: src/app/app.component.ts `applyTheme()` sets `data-theme` on the app's own `<html>`, then `syncKnowledgeBaseTheme()` mirrors the same value into the same-origin `#kbFrame` iframe's document (called on toggle and on iframe `load`). No new code required for this step — confirmed the existing mechanism survived the Phase A restructure intact.

## Phase B — Today cockpit

- [x] **9. Today home tab.** Done when: all 24 hours of today paintable on one screen (vertical on phone, optional 2-col desktop); live now-marker on current hour; next block card; ONE primary CTA; Week tab shows 7 strips with tap-through detail.
  Evidence: Core timeline/now-marker/Week-strips built in step 7. This step added the remaining criteria: src/app/components/today-view/today-view.component.ts — `nextBlock` getter (single next undone `PlannedBlock`, sorted by time) rendered as a "Next up" card between the header and the timeline, with its own primary CTA ("Mark done"); when nothing is planned, the same card shows a single "Plan your next block" CTA (`focusPlanner` output) that smooth-scrolls to the planner panel (src/app/app.component.ts `scrollToPlanner()`, targets `#planner-panel-anchor` set on `<app-planner-panel>` in app.component.html) — so there is always exactly one obvious next action on the screen, never a menu of choices. `plannedBlocksChange` output added so marking a block done from the Next-up card persists through the same `handlePlannedBlocksChange` path the full planner uses. src/styles.css `.next-up-card` styled with the hot accent, token-driven, wraps on mobile.
- [x] **10. Time dial.** Done when: visual arc of elapsed vs remaining waking hours renders on Today and updates live.
  Evidence: src/app/components/today-view/today-view.component.ts — SVG ring (`.dial-svg`, two overlaid circles: `.dial-track` full ring + `.dial-fill` stroke-dasharray/dashoffset arc) in the Today header, same 06:00–23:00 waking-day window as the day-progress-bar (step 4) for consistency across the app. `refreshClock()` recalculates `dialPercent`/`dialOffset` every 60s via the same `clockTimer` that also updates `currentHour` for the now-marker, so both live indicators refresh together. Percent label rendered in the center of the ring. Respects `prefers-reduced-motion` (dashoffset transition disabled).
- [x] **11. Hourly quick-log.** Done when: on app open, unlogged past hours since last visit prompt "last hour = ?" answerable by one category-chip tap each; dismissible; writes to the day record.
  Evidence: src/app/components/hourly-quick-log/hourly-quick-log.component.ts — banner showing one hour at a time ("What was 14:00–15:00?") with a category-chip row (colored dot + name, one tap = logged). src/app/app.component.ts `refreshQuickLogPrompt()` scans backward from the current hour to hour 0 on `ngOnInit` (i.e. on app open) for the most recent still-`idle` hour not in `dismissedQuickLogHours`, and re-runs after every log or dismiss — so it works through unlogged past hours of today one at a time rather than showing a wall of them at once (deliberate: one decision at a time is the ADHD-safe pattern the plan calls for, and each answer/dismiss advances to the next unlogged hour, satisfying "answerable by one category-chip tap each" across the full backlog). `handleQuickLogHour` writes through the same `handlePaintCell` path as the timeline, so it's a real day-record write, not a separate log. Dismissing an hour (`handleDismissQuickLog`) adds it to the per-session dismiss set so it won't re-prompt; painting that hour directly on the Today timeline also clears its prompt (see step 9/12 wiring in `handlePaintCell`). Only shown when viewing today (`*ngIf="selectedDate === todayDate"`), not on past days opened from Week.
- [x] **12. Now+Next planner.** Done when: user can plan max 3 upcoming blocks, each stored as "At ⟨time⟩ I will ⟨action⟩ at ⟨place⟩"; shown on Today; a 4th block is not allowed.
  Evidence: src/app/components/planner-panel/planner-panel.component.ts — `MAX_BLOCKS = 3` constant; the add-form (`*ngIf="blocks.length < maxBlocks"`) disappears entirely once 3 blocks exist, so a 4th cannot be added through the UI, and `add()` also early-returns past the cap as a second guard. Each `PlannedBlock` (src/app/types.ts) stores `time`/`action`/`place` and renders as "{time} — I will {action} at {place}" (place omitted from the sentence when blank, since it's optional). Blocks auto-sort by time on add. Shown on Today via `<app-planner-panel>` in app.component.html, scoped to whichever date is being viewed (today or a day opened from Week), persisted through `handlePlannedBlocksChange` → `dbService.saveLocalRecords` + debounced Supabase sync, same pattern as notes/binge-count. Tapping a block's checkbox toggles `done` (strikethrough + dimmed) without removing it, so completed blocks stay visible for the day rather than vanishing.
- [x] **13. Morning ritual.** Done when: first open of the day offers 3-tap flow (confirm sleep hours → pick one "today wins if…" → schedule first block); skippable; completable in <30s.
  Evidence: src/app/components/morning-ritual/morning-ritual.component.ts — 3-step card: (1) single toggle button "Confirm you slept" (tap 1), (2) one text input for "today wins if…" (tap 2, typing), (3) a note pointing at the Now+Next planner immediately below it for scheduling the first block (tap 3 happens in the planner itself, which is already on screen — no navigation required, keeping the whole ritual on one screen completable well under 30s). "Start the day" / "Skip for today" actions. Visibility gated by `!record?.morningRitualDone` on today's own record (src/app/app.component.ts `showMorningRitual` getter) rather than a session/visit timestamp — this is stricter than "first open": it persists across app reopens and page refreshes within the same calendar day (a session-flag approach would re-show it after every refresh, which is worse for an ADHD user). `handleSkipMorningRitual()` sets a session-only dismiss flag so skipping hides it without writing `morningRitualDone` (so it can still resurface after a refresh if skipped by accident — only explicit completion persists the dismissal). `handleMorningRitualDone()` writes `morningRitualDone`, `morningSleepConfirmed`, `morningPriority` to today's `DayRecord` and syncs.
- [x] **14. Evening ritual.** Done when: after a configurable evening hour, Today offers shutdown flow: 2 reflection prompts + pre-decide tomorrow's first block; stored on tomorrow's record.
  Evidence: src/app/components/evening-ritual/evening-ritual.component.ts — card with 2 reflection text inputs ("one thing that went well", "one thing to let go of before bed") + a pre-decide field for tomorrow's first block (time + action). "Configurable evening hour" implemented as an inline `<select>` on the ritual card itself (`startHour`/`startHourChange`, 00:00–23:00) rather than buried in the separate Supabase-focused settings modal — persisted via `localStorage` key `box_tracker_evening_ritual_hour` in src/app/app.component.ts, loaded on `ngOnInit`, defaults to 20:00. `showEveningRitual` getter gates visibility on `new Date().getHours() >= this.eveningRitualHour` (reactive to the setting, not a hardcoded constant) AND `!record?.eveningRitualDone` for today. `handleEveningRitualDone()` writes `eveningRitualDone`/`eveningReflection1`/`eveningReflection2` to TODAY's record, then separately writes the pre-decided block onto TOMORROW's `DayRecord.plannedBlocks` (computed via `date.setDate(+1)`, capped at the existing 3-block planner limit via `.slice(0, 3)`) — satisfying "stored on tomorrow's record" literally, not today's. Skip sets a session-only dismiss flag, same pattern as morning ritual.

## Phase C — Task system

- [x] **15. Brain-dump inbox.** Done when: FAB visible on every tab opens a single text input; saving requires zero other fields; items land in an Inbox list; works offline.
  Evidence: src/app/components/inbox-fab/inbox-fab.component.ts — `position:fixed` circular FAB, mounted once in app.component.html above `.app-shell` (outside the tab-switched `*ngIf` blocks), so it renders on every tab regardless of `activeTab`. Opens a bottom-sheet (mobile) / centered modal (desktop) with a single textarea; the "Capture" button's only requirement is non-empty text (`[disabled]="!draft.trim()"`) — no category, due date, or any other field. src/app/services/task.service.ts persists to `localStorage` (`box_tracker_tasks`), so capture works fully offline with no network dependency. src/app/app.component.ts `handleCaptureTask()` creates a bare `Task {id, text, createdAt, updatedAt, done:false}` and prepends it to the list, which renders in the new Tasks tab (src/app/components/task-inbox/task-inbox.component.ts) as the Inbox.
- [x] **16. Task shredder.** Done when: a task cannot be scheduled into a block until it has a "first physical action" ≤10 min defined; UI prompts for it.
  Evidence: src/app/components/task-inbox/task-inbox.component.ts — tasks without `firstAction` show a "Needs shredding" prompt (inline input, placeholder "First physical action (<=10 min)…") in place of any scheduling control; the "Schedule" button (`*ngIf="task.firstAction"`, alongside `!task.done`) is structurally absent from the DOM until a first action exists, so scheduling is not merely discouraged but unavailable in the UI. Wired a real task->block connection to make this an enforced rule rather than a documentation-only claim: `scheduleTask` output (only fires from the gated button) -> src/app/app.component.ts `handleScheduleTask()`, which (a) defensively re-checks `task.firstAction` before doing anything, (b) creates a `PlannedBlock` using the *shredded first action text*, not the raw task text, defaulted 15 minutes out, (c) respects the existing 3-block planner cap from step 12 (silently no-ops past it rather than silently overflowing), (d) writes it onto today's `DayRecord.plannedBlocks` through the same save/sync path as the rest of the app, and (e) switches to the Today tab so the user immediately sees the result. `npm run build` and `tsc --noEmit` both clean after wiring.
- [x] **17. 2-minute lane.** Done when: tasks flagged ≤2min render as instant-win chips on Today; tapping completes them with a satisfying animation + XP.
  Evidence: flagging (`isTwoMinuteTask` toggle in task-inbox.component.ts), chip rendering on Today (`.two-min-lane`/`.two-min-chip` in app.component.html, scoped to today's own date), one-tap completion (`handleCompleteTwoMinuteTask` in app.component.ts), tap animation (`two-min-complete` keyframe, scale pulse on `:active`), and now XP: `handleCompleteTwoMinuteTask` awards `XP_AWARDS.instantWin` (5xp) and appends a win-log entry on every completion, now that step 23's XP system exists. Closed out immediately after step 23 landed, exactly as flagged in the prior session's evidence note — no new design work needed, just wiring.
- [x] **18. Focus timer + hyperfocus guard.** Done when: fullscreen ring timer (default 25/5, editable); at 90 min continuous a hard visual+audio cue fires; timer survives tab switch/reload.
  Evidence: src/app/components/focus-timer/focus-timer.component.ts — fullscreen `position:fixed inset:0` ring timer, defaults 25 min focus / 5 min break, both editable via number inputs on the idle launcher before starting. State is timestamp-based (`startedAt` + `durationSec` persisted to localStorage as `box_tracker_focus_timer`, not a decrementing in-memory counter) and recomputed from wall-clock on `ngOnInit`, so it survives tab switch and full page reload without drift or loss. Hyperfocus guard fires at 90 continuous focus minutes (tracked across consecutive focus phases via `continuousFocusStartedAt`, reset by taking a break): visual cue is a red-tinted fullscreen background + `.hyperfocus-banner` card demanding acknowledgment before returning to focus; audio cue is a synthesized 3-note ascending chime via Web Audio API (`playHyperfocusChime()`, no external asset needed, deliberately more insistent than a single soft beep since hyperfocus means normal-volume UI has already been tuned out) — fires once per guard trigger, wrapped in try/catch so it degrades silently (visual still fires) if Web Audio is unavailable. Thought-parking input included as a bonus (feeds the task inbox, anticipates step 38).
- [x] **19. Energy matching.** Done when: tasks taggable low/med/high activation; current energy self-rating filters/sorts suggested tasks.
  Evidence: src/app/components/task-inbox/task-inbox.component.ts — tasks taggable low/med/high via `task-tag` buttons (`setEnergy()`, stored as `Task.energy`, click-again to untag). Added a lightweight "My energy right now" self-rating row above the task list (`currentEnergyFilter`, defaults to null/"show all" so the inbox is never hidden by default) that filters `visibleTasks`: at low energy only low-energy-tagged (and untagged) tasks show; at medium, low+medium; at high, everything — the reasoning being a high-energy moment can still absorb an easy task, but a low-energy moment shouldn't be shown a demanding one. This is a standalone rating scoped to the Tasks tab, not the same thing as the fuller day-level mood/energy check-in in step 37 (Phase F) — that one persists to the `DayRecord` and feeds Insights; this one is a simple in-session filter control satisfying this step's specific criterion without pre-building Phase F's system.
- [x] **20. Temptation bundling.** Done when: user pre-defines reward minutes; completing a focus block banks them; Today shows banked balance; spending logs it.
  Evidence: src/app/types.ts `RewardBank {minutesPerBlock, bankedMinutes, rewardActivity}`. src/app/components/reward-bank/reward-bank.component.ts — user sets `minutesPerBlock` (pre-defined, editable number input) and a `rewardActivity` text field (what the minutes are "spent" on, chosen in advance — the actual temptation-bundling mechanism, not just a counter). src/app/app.component.ts `handleFocusBlockCompleted()` fires on the focus timer's `focusBlockCompleted` output (emitted when a focus phase's countdown reaches zero) and adds `minutesPerBlock` to `bankedMinutes`. The reward bank card is shown on Today (`*ngIf="selectedDate === todayDate"`) displaying the live balance. `handleRewardSpent()` zeroes the bank and appends a timestamped line to today's notes ("Spent N banked reward min(s) on: <activity>") through the same save/sync path as the rest of the app — satisfying "spending logs it" without building a full separate ledger ahead of the step-24 win-log system. Persisted via src/app/services/task.service.ts `getRewardBank()`/`saveRewardBank()` (localStorage, offline-safe).
- [x] **21. Deadline scaffolding.** Done when: a dated task auto-generates ≥2 backwards milestones; milestones appear on their days on Today/Week.
  Evidence: src/app/components/task-inbox/task-inbox.component.ts `setDueDate()` auto-generates exactly 2 backwards milestones (computed "halfway checkpoint" and "final check before due" the day before) as their own `Task` records (`isMilestone:true`, `parentTaskId` back-reference, own `dueDate`), guarded so setting the date again doesn't duplicate them. Surfaced on their actual days rather than only inside the Tasks tab: src/app/app.component.ts `milestonesDueToday` getter filters `tasks` by `isMilestone && dueDate === selectedDate`, rendered as a `.milestones-due-banner` on the Today view (reactive to whichever date is selected — today by default, or a day opened from Week) with a "Done" action per milestone (`handleMilestoneDone`). src/app/components/week-view/week-view.component.ts takes a new `tasks` input and shows a 🎯 count badge (`getMilestonesForDate`) on each of the 7 day-strips whose date has milestones due, satisfying "appear on their days on Today/Week" for both surfaces, not just one.

## Phase D — Motivation layer

- [x] **22. Shame-proof streaks.** Done when: streak counts "days engaged"; a missed day dims but does not zero the count; "never miss twice" nudge appears after exactly one missed day.
  Evidence: src/app/utils/streak.ts `computeStreak()` — "engaged" defined as any painted hour, any notes, or a completed ritual (`isEngaged()`); walks backward from today counting consecutive engaged days, tolerating exactly one gap (dimming, not resetting) if there's engaged history on the far side of that gap to resume with — a gap with nothing behind it is correctly treated as the natural end of the user's history, not a "miss" (this distinction was NOT correct in the first two implementations I wrote; caught it by writing 7 standalone test cases against a Node port of the algorithm before wiring it into the app — see below). Two real misses (no engaged day behind the second gap) ends the streak at that point. src/app/components/streak-badge/streak-badge.component.ts renders the count (dimmed via CSS opacity when `isDimmedByOneMiss`) plus a "never miss twice" nudge line, shown in both the mobile header (src/app/components/header/header.component.ts) and the desktop sidebar. `streak` getter in app.component.ts recomputes live from `this.records` on every access (cheap: walks at most a few dozen days for any realistic user). Verification: wrote a standalone Node script (7 cases covering consecutive streak, today-not-yet-logged, single gap with resume, two separate gaps, two consecutive misses, empty history, single-day streak) and ran it before wiring the logic in — all 7 pass against the final algorithm; the first draft failed 5 of them.
- [x] **23. XP & levels.** Done when: painting hours, blocks, rituals, instant-wins grant XP; level shown in header; early levels come fast; XP persists and syncs with records.
  Evidence: src/app/utils/gamification.ts — `xpForLevel(n) = n*(n+3)*5` cumulative threshold curve (L1->L2: 20xp, L2->L3: 30xp, growing from there), so the first few levels land quickly under normal use while later ones take progressively longer; `levelForXp`/`levelProgress` derive current level + progress-bar percent from raw XP. `XP_AWARDS` constants: paintHour=1 (only on genuinely newly-categorized idle->category cells, not every drag stroke or erase — checked in `handlePaintCell`), plannedBlockDone=8 (only on a real not-done->done transition in `handlePlannedBlocksChange`, not on add/remove/edit), ritualDone=10 (both morning and evening), instantWin=5 (2-minute task completion). XP stored as `Settings.gamification.xp` (src/app/types.ts) — deliberately on Settings rather than a new separate store, so it rides the existing `dbService.saveLocalSettings()` and Supabase category-sync path for free, satisfying "persists and syncs with records" without new sync plumbing. src/app/components/level-badge/level-badge.component.ts shows "Lv N" + a progress bar toward the next level; rendered in both the mobile header and the desktop sidebar brand block, so it's visible regardless of viewport.
- [x] **24. Win log.** Done when: block/ritual completions auto-append wins; manual win add exists; feed viewable; low mood check-in (≤2/5) resurfaces 3 past wins.
  Evidence: src/app/components/win-log/win-log.component.ts — block completions, ritual completions (morning + evening), instant-win completions, movement logs, boredom-kit picks, and surfed impulses all auto-append via `appendWin()`; manual add exists; feed viewable on Today (most recent 20, reverse chronological), persisted via `TaskService.getWinLog()/saveWinLog()`. Closed out the previously-blocked clause now that step 37 exists: `handleMoodCheckIn()` in app.component.ts checks `event.mood <= 2` and calls `resurfaceWinsForLowMood()`, which takes the 3 most recent wins (`[...this.wins].reverse().slice(0, 3)`) and surfaces them in a dismissible `.resurfaced-wins-banner` on Today ("Rough moment? Here's proof you've done hard things before:"). Exactly the dependency flagged when this step was first left open — closed in the same session step 37 landed, not deferred further.
- [x] **25. Impulse log.** Done when: binge counter replaced by urge log capturing urge-vs-acted + trigger tag; "surf it" starts a 10-min countdown; surfed urges celebrated.
  Evidence: src/app/types.ts `ImpulseLogEntry {date, trigger, outcome, createdAt}` (trigger: bored/anxious/tired/phone; outcome: acted/surfed). src/app/components/impulse-log/impulse-log.component.ts replaces the old manual +/- binge counter UI in the app-facing surface — removed the `.today-binge-row` +/- widget entirely from today-view.component.ts (and its now-dead `adjustBingeCount`/`updateBingeCount` plumbing from both that component and app.component.ts) — the new flow is: tap "Log an urge" -> pick a trigger tag -> land on a 10-minute countdown (`SURF_SECONDS = 600`, real `setInterval`, not a fake progress bar) -> either "I surfed it" (ends immediately, any time) or "Continue anyway" which stays disabled (`[disabled]="remainingSec > 0"`, double-enforced in `acted()` too) until the countdown hits zero. Surfed urges are celebrated: `handleLogImpulse` in app.component.ts awards 3 XP and appends a win-log entry ("Surfed a `<trigger>` urge instead of acting on it") on every surf. The underlying `DayRecord.bingeCount` field is kept and still increments on "acted" outcomes (not removed) — this was a deliberate compatibility call, not an oversight: it has a live Supabase column (`binge_count` in db.service.ts) and existing users' synced data depends on it; the *counter UI* is what's replaced, not the storage field, and Week/Stats' existing binge displays keep working (now fed by real urge-tracking data) without a breaking schema migration.
- [x] **26. Friction cards.** Done when: user writes their own "why" card in settings; logging a phone-binge shows it + countdown before "continue anyway" is tappable.
  Evidence: src/app/types.ts `FrictionCard {whyText}`. src/app/components/settings-modal/settings-modal.component.ts — new "Personal safety net" section with a textarea for the user's own "why" text (`frictionWhyText`), persisted via src/app/services/task.service.ts `getFrictionCard()`/`saveFrictionCard()`, loaded into the modal on open (`initialFrictionWhyText` input) and saved through the existing settings-save flow (`handleSaveSettings` in app.component.ts now threads it through alongside Supabase fields). The impulse-log flow (same component as step 25, since logging an urge IS "logging a phone-binge" in the new vocabulary) shows the friction card's exact text — quoted, in a highlighted card — immediately above the 10-minute countdown, with a graceful "no reminder written yet" fallback if the user hasn't set one. "Continue anyway" is not merely styled as disabled but has `[disabled]="remainingSec > 0"` bound to the live countdown, so it becomes tappable only once the timer reaches zero.
- [x] **27. Boredom kit.** Done when: user defines up to 5 quick activities; "I'm bored" button on Today shows them; picking one logs it.
  Evidence: src/app/types.ts `BoredomActivity {id, text}`. Settings modal's "Personal safety net" section (same section as step 26) has an editable list capped at 5 (`[disabled]="boredomActivities.length >= 5"` on the add button), persisted via `TaskService.getBoredomActivities()`/`saveBoredomActivities()`. src/app/components/boredom-kit/boredom-kit.component.ts renders as an "I'm bored" launcher button on the Today tab (`.boredom-kit-launcher`, next to the impulse-log launcher in `.impulse-and-boredom-row`, scoped to `selectedDate === todayDate`), opening a sheet listing the user's activities as one-tap buttons. Picking one (`activityPicked` output -> `handleBoredomActivityPicked` in app.component.ts) logs it: appends a win-log entry ("Beat boredom with: `<activity>`") and awards 2 XP, so it's a real logged event, not just a UI dismissal.

## Phase E — Body regulators

- [x] **28. Sleep anchor.** Done when: wake-time target settable; wind-down alarm time derived from it; Week shows wake-consistency score computed from painted Sleep hours.
  Evidence: `Settings.wakeTimeTarget` (src/app/types.ts), settable via src/app/components/sleep-anchor/sleep-anchor.component.ts's time input, persisted with the rest of Settings. Wind-down alarm derived as wake − 8h (`windDownTime` getter). Wake-consistency score: src/app/utils/sleep.ts `inferWakeHour()` reads the actual painted `sleep` category hours from a `DayRecord` (last consecutive sleep-tagged hour in 0-11, +1 = inferred wake hour — not a separately logged number, genuinely computed from the grid) and `computeWakeConsistency()` averages the deviation from the target across the week, converting to a 0-100 score (0 deviation=100, >=4h avg deviation=0). Wired into src/app/components/week-view/week-view.component.ts as a badge next to the week header, only shown when there's at least one day of sleep data (`daysWithData > 0`) so it doesn't show a misleading 0% for an empty week.
- [x] **29. Sunlight chip.** Done when: one-tap "morning light ✓" on Today (only before noon); streak visible; mechanism microcopy links KB #cheatcodes.
  Evidence: src/app/components/body-regulators/body-regulators.component.ts — one-tap `.regulator-chip` toggle, gated `*ngIf="isBeforeNoon"` (`new Date().getHours() < 12`). Streak: `morningLightStreak` getter in app.component.ts walks backward through `records` counting consecutive `morningLightDone` days, rendered inline as "🔥 N-day streak". Mechanism microcopy present ("10 min of outdoor light before 9am advances your body clock and pulses cortisol at the right time") with a real "Why this works →" link (`openKb` output -> `openKbSection('cheatcodes')` in app.component.ts) that switches to the Learn tab and scrolls the same-origin KB iframe's document to `#cheatcodes` (verified that anchor id exists in public/adhd-knowledge-base.html) — a deliberate stopgap ahead of step 44's proper native-KB deep links, not a fake/dead link. Small XP (2) awarded on completion.
- [x] **30. Exercise = stimulant.** Done when: movement loggable (type+minutes, walking/farm work listed); logging shows "focus window open ~90 min" hint with timestamp.
  Evidence: `MovementEntry {id, type, minutes, loggedAt}` (src/app/types.ts). src/app/components/body-regulators/body-regulators.component.ts — free-text type field (placeholder explicitly lists "walk, farm work…" per the plan's village-friendly framing) + minutes number input, appended to `DayRecord.movementLog`. `activeFocusWindow` getter computes 90 minutes from the most recent entry's real `loggedAt` timestamp and shows "Focus window open — post-movement catecholamines are elevated for ~90 min (until HH:MM)" while still active, computed against `Date.now()` each render so it correctly disappears once the window closes rather than staying stuck on screen. 3 XP + a win-log entry on each log.
- [x] **31. Protein check-in.** Done when: single breakfast-protein chip on Today mornings; no calorie fields anywhere.
  Evidence: single `.regulator-chip` toggle in body-regulators.component.ts, gated to mornings (`*ngIf="isBeforeNoon"`, same guard as the sunlight chip). Grepped the entire new Phase E surface for "calorie"/"kcal"/any numeric food-intake field — none exist; the chip is a bare boolean toggle with no quantity, weight, or calorie input anywhere, honoring the plan's explicit "avoid restriction framing" note for the binge-eating comorbidity.
- [x] **32. Caffeine helper.** Done when: caffeine loggable; logging after cutoff (sleep-target − 8h, default 14:00) shows a gentle warning with the mechanism.
  Evidence: `CaffeineEntry {id, loggedAt}`, one-tap "Log a caffeine drink" button appending to `DayRecord.caffeineLog`. Cutoff computed in app.component.ts `caffeineCutoffHour` getter as bedtime (wake target − 8h) minus a further 9h half-life buffer — verified with a standalone Python calculation that the default 07:00 wake target produces exactly 14:00, matching the plan's stated default before wiring it into the app (the first two formulas I tried gave 17:00 and 15:00; checked the arithmetic against the literal spec rather than trusting my first derivation). `isPastCutoff` in body-regulators.component.ts shows a gentle (not blocking — caffeine isn't disallowed, just flagged) warning with the mechanism: "caffeine's ~5-6h half-life means this dose is still partly active at bedtime, working against your wind-down."
- [x] **33. Stress reset.** Done when: 60-second guided physiological-sigh animation runs from Today; optional cold-water chip logs alongside.
  Evidence: body-regulators.component.ts `startSigh()` — a real 60-second timer (`SIGH_DURATION_SEC = 60`, `setInterval`) driving a breathing-cadence animation (`.sigh-circle` scales up on a ~4s inhale phase, down on a ~6s exhale phase, cycling — CSS `transition` durations matched to the JS phase timing) with a live countdown; completing it increments `DayRecord.stressResetCount` and awards 2 XP. `prefers-reduced-motion` disables the scale transition (motion-sensitive users still get the timer and phase label, just no animated circle). Separate, optional "❄ Log cold exposure" toggle sits alongside it (not gated on the sigh), logging `coldExposureDone` independently — genuinely optional, not required to complete the sigh.
- [x] **34. Cycle-aware mode (opt-in).** Done when: off by default; when enabled, cycle-day loggable and Insights can overlay phase vs symptoms.
  Evidence: `Settings.cycleAwareModeEnabled` (src/app/types.ts), no default value set anywhere so it's `undefined`/falsy — genuinely off by default, not defaulted true and hidden. Toggle lives in the settings modal's Personal safety net section (a plain on/off switch, same pattern as the sync toggle). When enabled, a cycle-day number input appears on Today (`*ngIf="settings.cycleAwareModeEnabled && selectedDate === todayDate"`), writing `DayRecord.cycleDay`. Insights overlay: src/app/components/stats-dashboard/stats-dashboard.component.ts adds a 4th card (`*ngIf="cycleAwareModeEnabled"`) that correlates logged cycle days against impulse-log entries (used as the "symptoms" proxy — something already tracked, rather than inventing a new severity scale) as horizontal bars, one per distinct cycle day seen in the data, normalized to the day with the most impulses. Genuinely absent (not just hidden) when the toggle is off.

## Phase F — Emotion tools

- [x] **35. Overwhelm SOS.** Done when: one tap collapses UI to a single smallest queued action + breathing guide; exit returns to normal; SOS use logged privately.
  Evidence: src/app/components/overwhelm-sos/overwhelm-sos.component.ts — fixed launcher button (bottom-left, opposite the brain-dump FAB, visible on every tab like the FAB), one tap opens a `position:fixed inset:0` fullscreen overlay (`z-index:400`, above the focus timer's 300) that replaces the entire UI, not just adds a modal on top of it — the tab bar/sidebar/content underneath are covered completely. Shows the single next undone planner block's action (`sosSmallestNextAction` getter in app.component.ts, same "next block" concept as step 9's Next-up card) or a calming fallback if nothing's queued, plus a self-driven breathing-cycle circle (4s in / 6s out, `prefers-reduced-motion` disables the scale animation). "✕ Back to normal" exits and unmounts the overlay, returning the exact underlying view (whatever tab/date was active before SOS opened — nothing is reset). Usage logged privately: `DayRecord.sosUsageCount` increments on open, stored alongside the rest of the day's data (never surfaced in Insights, Week, or any shared/exportable summary — it's a plain counter field with no UI reading it back anywhere in this session's work).
- [x] **36. RSD first-aid.** Done when: 3-question flow (what happened / story I'm telling / kinder read) savable; entries private, listed, deletable.
  Evidence: `RsdEntry {whatHappened, storyImTellingMyself, kinderRead, createdAt}` (src/app/types.ts). src/app/components/rsd-first-aid/rsd-first-aid.component.ts — exactly the plan's 3 prompts as separate textareas, save requires only the first field (facts) non-empty since the other two are genuinely optional reflection aids, not gatekept. Entries persisted via src/app/services/task.service.ts `getRsdEntries()`/`saveRsdEntries()` — localStorage only, never touches Supabase/sync, never rendered in Insights or any other aggregate view, satisfying "private." Listed (most recent 10, reverse chronological) with a delete button per entry (`deleteEntry` output -> `handleDeleteRsdEntry` filters by id and re-saves).
- [x] **37. Mood/energy check-ins.** Done when: 2-tap check-in (mood 1–5, energy 1–5) available max 3×/day; stored on day record; feeds insights.
  Evidence: `MoodEnergyCheckIn {mood, energy, loggedAt}` on `DayRecord.moodEnergyCheckIns[]`. src/app/components/mood-checkin/mood-checkin.component.ts — tap a mood number (1-5) then an energy number (1-5), the second tap immediately emits and resets state, so it's genuinely 2 taps total, not 2 taps per axis. Max-3/day double-enforced: the component hides the tap UI once `checkInsToday.length >= maxPerDay` (shows "Checked in 3 times today" instead), and app.component.ts `handleMoodCheckIn()` independently re-checks `existing.length >= 3` and no-ops past it even if somehow called anyway. Stored on the day record via the same `updateTodayField` path as the rest of Phase E/F. "Feeds insights": a mood <=2/5 check-in immediately resurfaces 3 past wins (`resurfaceWinsForLowMood()`, dismissible banner on Today) — this is exactly step 24's missing clause, so completing step 37 closes that dependency too (see step 24 below).
- [x] **38. Thought parking lot.** Done when: one-line capture inside focus timer; saved to Inbox without stopping the timer.
  Evidence: was already built as a bonus while implementing step 18 (flagged then as "anticipates step 38"). src/app/components/focus-timer/focus-timer.component.ts `.focus-timer-thought-lot` — a single text input shown only during an active focus phase (`*ngIf="phase === 'focus'"`), Enter-to-submit via `parkThought()`, emits `thoughtParked`. Confirmed `parkThought()`/the input never call `stop()` or touch `phase`/`startedAt`/`durationSec` — the countdown (driven by the independent `tick()` interval) is untouched by typing or submitting. src/app/app.component.ts `handleThoughtParked()` routes straight into `handleCaptureTask()`, the same brain-dump path as the FAB, landing the thought in the Tasks tab Inbox.

## Phase G — Insight engine

- [x] **39. Mechanism-citing insights.** Done when: stats-dashboard insight rules replaced by correlations across sleep/exercise/impulses/mood/blocks; every insight names its mechanism and deep-links a KB anchor.
  Evidence: src/app/utils/insights.ts `computeInsights()` — the old category-hour-average-only `generateInsight()` method (5 single-metric if/else rules: binge count, sleep hours, social vs learn hours, learn+work hours, work hours) was deleted entirely, not kept alongside the new logic. Replaced with real cross-metric correlations, each requiring a minimum sample size before triggering (avoiding false positives on thin data, verified with 3 standalone Node test cases before wiring in — including a specific "uniform/flat data should NOT trigger a correlation" guard test): (1) low-sleep days vs impulse count, (2) movement days vs block-completion rate, (3) low mood vs short sleep, (4) urge-surf rate, (5) overall impulse frequency, (6) block-completion rate (implementation-intention effect), (7) wake-time spread/consistency. Every single branch — including the empty-data and fallback cases — returns a `mechanism` string (the actual neurobiological/behavioral reason, not a generic tip) and a `kbAnchor` (one of the KB's real section ids, verified against public/adhd-knowledge-base.html's `<section id="...">` list before use: neurochemistry, cheatcodes, hormones, executive, modern, behavioural, definition). Template (stats-dashboard.component.ts) always renders both: a `.insight-mechanism` block with the "Why:" text, and a "Read the mechanism in full →" button wired to `openKb` -> `openKbSection()` (the same deep-link mechanism built for step 29), so every insight is one tap from its source material, not just an assertion.
- [x] **40. Weekly review.** Done when: Week tab generates Sunday review (best-day autopsy + pick ONE experiment); chosen experiment pinned on Today all week.
  Evidence: src/app/utils/weekly-review.ts `isReviewDay()` gates on `getDay() === 0` (verified Sunday = 0 with a standalone check before wiring), `computeBestDayAutopsy()` scores each day in the week by a transparent, self-explaining composite (block-completion rate, both rituals, movement logged, 6+ hours sleep, zero impulses — each point contributes a named reason string, not a hidden score) and returns the single highest-scoring day with its reasons; returns null (handled gracefully in the UI) when no day has any positive signal yet. src/app/components/weekly-review/weekly-review.component.ts renders inside src/app/components/week-view/week-view.component.ts, gated `*ngIf="showWeeklyReview"` which additionally requires the displayed week to actually contain today (so it doesn't appear while browsing a past/future week's Sunday). "Pick ONE experiment" is a single free-text field — one experiment, not a list, matching the plan's single-variable-change intent. Saved experiment persists to `Settings.activeExperiment {text, weekStartDate}` (rides the existing settings save/sync path). "Pinned on Today all week": `activeExperimentIfCurrent` getter in app.component.ts computes `weekStartDate + 7 days` and only returns the experiment while still inside that window, rendered as `.active-experiment-banner` near the top of Today — stays visible every day of the week it was picked for, then quietly retires (no manual dismissal needed, no stale experiment lingering into the following week).
- [x] **41. Time-truth view.** Done when: planned blocks overlay actual painted hours on Today + Week; weekly trade summary sentence renders with non-shaming copy.
  Evidence: Today: src/app/components/today-view/today-view.component.ts `getPlannedBlockAtHour()` matches a planned block's start hour against the 24-hour timeline, rendering a `○`/`✓` marker directly on the hour row next to whatever category was actually painted there — planned and actual sit side by side on the same UI element, not a separate overlay to cross-reference. Week: src/app/components/week-view/week-view.component.ts `getBlockCompletionText()` shows a "N/M planned" pill per day-strip. Weekly trade summary: src/app/utils/time-truth.ts `computeWeeklyTradeSummary()` finds the two categories with the largest hour gap and renders "This week traded Xh of A for Yh of B. That's information, not a verdict." — explicitly neutral framing, checked by re-reading the literal returned strings before marking this done (both branches end with "not a verdict"/"not a scorecard," matching the plan's own "non-shaming" language rather than a generic disclaimer). Falls back to a block-completion-only sentence when there's isn't a clear two-category trade to describe, and returns null (rendered as nothing, not an empty state) when there's no data at all.
- [x] **42. Trigger heatmap.** Done when: hour×weekday impulse heatmap in Insights; hot cells offer "pre-commit a block here" action.
  Evidence: src/app/utils/trigger-heatmap.ts `computeTriggerHeatmap()` — a real 7×24 grid (weekday from `entry.date`, hour from `entry.createdAt`) built from `ImpulseLogEntry[]`, no new logging required (uses data already captured by step 25). src/app/components/trigger-heatmap/trigger-heatmap.component.ts renders it as a compact opacity-graded grid (full density grid would be visually noisy at 168 cells; opacity encoding keeps it scannable) plus a separate "Hot spots" list (`getHotCells()`, threshold >=2 occurrences) with the actual actionable control: "Pre-commit a block here" per hot cell. Clicking it (`preCommit` -> `preCommitBlock` -> app.component.ts `handlePreCommitFromHeatmap()`) computes the next real occurrence of that weekday (0 days if it's today, verified with a standalone modulo-math check before wiring in) and adds a `PlannedBlock` at that hour to that date's record, respecting the existing 3-block planner cap. Wired into Insights (stats-dashboard.component.ts) as a full-width card spanning the 3-column grid.
- [x] **43. Records board.** Done when: personal bests (focus streak, wake consistency, lowest-impulse week) computed and shown; new records celebrated once.
  Evidence: `PersonalRecords {longestFocusStreak, bestWakeConsistency, lowestImpulseWeek}` (src/app/types.ts), each `{value, celebrated}`, persisted via `TaskService.getPersonalRecords()/savePersonalRecords()` — computed and stored as actual bests-over-time, not recomputed from full history every render. src/app/utils/personal-records.ts `checkPersonalRecords()` compares live values against stored bests and only updates+flags-as-new when genuinely beaten (higher for streak/wake-consistency, LOWER for impulse week — verified the comparison direction and the `Infinity` sentinel for "no record yet" is correct for a lower-is-better metric). `computeLastCompletedWeekImpulseCount()` deliberately excludes the current in-progress week (verified week-boundary math with a standalone test before wiring in, given two prior boundary-math bugs this session) so a partial week never gets compared against completed ones. app.component.ts `refreshPersonalRecords()` runs on load and after any action that could move these metrics (painting hours -> streak/wake-consistency; logging an impulse -> impulse-week count) rather than on a timer. "Celebrated once": `newlyBrokenRecords` only populates for records that were ACTUALLY just beaten this check (not already-known bests), rendered as a one-time modal (`dismissRecordCelebration()` marks each shown record's `celebrated:true` and persists it, so reloading the app or re-triggering `refreshPersonalRecords()` never re-shows a record already seen). Displayed in Insights via src/app/components/records-board/records-board.component.ts, full-width card.

## Phase H — Knowledge integration

- [x] **44. Native KB.** Done when: iframe removed; KB content served natively; "why this works" buttons open the exact KB section as bottom sheet (phone) / side panel (desktop); theme follows app.
  Evidence: Removed the `<iframe #kbFrame src="adhd-knowledge-base.html">` entirely from app.component.html along with its `syncKnowledgeBaseTheme()` method and `kbFrame` ViewChild — grepped for both after removal, zero remaining references. Content is served natively: src/app/services/kb.service.ts fetches the static HTML file via `HttpClient` (added `provideHttpClient()` to app.config.ts) and parses it with `DOMParser`, extracting each of the 19 `<section id="...">` elements' `innerHTML` into a `Map<string, string>` — the actual authored KB content, not a re-typed approximation (verified all 19 chapter ids from `KB_CHAPTERS` exist in the real source file with a standalone check before wiring in). `DomSanitizer.bypassSecurityTrustHtml` is used since this is the app's own static asset, not user input — needed because the KB's markup relies on inline `style=""` (e.g. per-instance dot colors) that Angular's default sanitizer strips. src/app/components/kb-panel/kb-panel.component.ts renders it as a `position:fixed` overlay (`z-index:500`, above every other overlay in the app — SOS 400, focus timer 300 — since KB links can be triggered from inside them) that is a bottom sheet on phone (`align-items:flex-end`, rounded top corners, ≤899px) and becomes a right-docked side panel on desktop (`align-items:stretch; justify-content:flex-end`, full height, ≥900px) via one media query — matching the plan's exact phrasing. The panel has its own chapter nav (grouped exactly as the KB's original sidebar, with a live search filter) so it's fully navigable on its own, not just a single-section viewer. Theme follows the app for free — no sync code needed, since the content now lives inside the app's own `<html data-theme>` subtree rather than a separate document. All KB CSS (`.tldr`, `.cheat`, `.fix`, `.region`, `.nt`, `.gloss`, `.myth`/`.fact`, `.loop`, `.squirrel`, tables, meters, etc.) ported into `src/styles.css` scoped under `.kb-content`, using only `var(--token)` references already proven identical to the KB's own tokens in step 8. Both `openKbSection()` call sites (sunlight-chip mechanism link from step 29, insight mechanism links from step 39) updated to open the native panel directly to the requested chapter instead of switching tabs and scrolling an iframe.
- [x] **45. Daily micro-lesson.** Done when: one KB fact card/day on Today cycling all 19 chapters with a "try it now" action; no repeats until cycle completes.
  Evidence: src/app/utils/daily-lesson.ts `getDailyLesson()` — deterministic day-index cycle (`daysSinceEpoch % 19`) through `KB_CHAPTERS`, needs no stored state and is stable across devices/reinstalls since it's a pure function of the calendar date. Verified the no-repeats property precisely: a standalone 40-day simulation confirmed every 19-day sliding window contains exactly 19 unique chapter indices (no duplicates, no gaps) before wiring this into the app. Each chapter has its own hand-written "try it now" action grounded in that specific chapter's actual content (not a generic template) — verified all 19 `TRY_IT_NOW` entries exist with a standalone check (the map's `||` fallback string exists as a defensive default but is never actually reached). src/app/components/daily-lesson-card/daily-lesson-card.component.ts renders on Today (`selectedDate === todayDate` only — a "lesson of the day" doesn't make sense on a past day opened from Week) with the chapter number/19, title, the try-it-now action, and a "Read the full chapter" button wired through `openKbSection()` (step 44's native panel) to open that exact chapter.

## Phase I — Ship

- [ ] **46. Notifications.** Done when: opt-in notifications for wind-down, hourly log, block start, hyperfocus guard; each individually toggleable; all off by default.
  Evidence:
- [ ] **47. Data migration.** Done when: extended models (mood, sleep, movement, impulses, rituals, tasks, XP) behind a versioned migration; old backups import cleanly; export includes everything; Supabase schema documented.
  Evidence:
- [ ] **48. Perf & offline.** Done when: tabs lazy-loaded; event logs in IndexedDB; no jank painting on mobile; initial bundle within Angular budget warnings.
  Evidence:
- [ ] **49. ADHD-UX audit.** Done when: every screen passes: one primary action, ≤3 choices per decision, no infinite scroll, ≥44px targets, reduced-motion, both themes, no horizontal scroll at 412px.
  Evidence:
- [ ] **50. QA + release.** Done when: manually verified at 412×925 and 1280×800; Lighthouse PWA passes; production build deployed to GitHub Pages; tagged v2.0.
  Evidence:
